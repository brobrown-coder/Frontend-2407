// for of 반복문
// for in 반복문

// for 반복문

const 배열 = ['바나나', '사과', '귤']

for (const 요소 of 배열) {
    console.log(요소);
}


for (const index in 배열) {
    console.log(index, 배열[index]);
}

for (let i = 0; i < 5; i++) {
    const element = array[i];
    
}